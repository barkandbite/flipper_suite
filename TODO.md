# Flipper Suite — Maintenance TODO

## Review Rotation Log

| Date       | App Reviewed     | Findings                                                    |
|------------|------------------|-------------------------------------------------------------|
| 2026-03-30 | uart_sniff       | Batch-read fix applied to worker thread. Code otherwise clean. |
| 2026-03-31 | subghz_jammer    | Added CC1101 hw error screen (was silent failure). Code otherwise clean. |
| 2026-03-31 | subghz_spectrum  | Removed stale fap_icon_assets. Logged HAL API migration. Code otherwise clean. |
| 2026-04-01 | flipperpwn       | Fixed EXFIL_USB COM port filtering bug (Windows 11). Logged marauder get_* race condition for future fix. |
| 2026-04-02 | nfc_fuzzer       | Fixed SD card log truncation (256-byte buf for 1555-char lines), progress bar uint32 overflow, volatile thread safety, redundant free(NULL). |
| 2026-04-03 | badusb_pro       | Added malloc NULL check in entry point, removed dead code in input handler. Code otherwise clean after thorough trace. |
| 2026-04-04 | hid_exfil        | Fixed USB config clobbered on repeated runs (usb_prev overwritten with HID config). Added malloc NULL check. Code otherwise clean after full trace. |
| 2026-04-05 | spi_flash_dump   | Fixed progress bar uint32 overflow for >37MB chips. Added malloc NULL checks. Fixed README dump path mismatch. Settings Back UX logged. |
| 2026-04-06 | ccid_emulator    | Added malloc NULL check. Fixed log_count uint16 overflow (wraps after 65535 APDUs → display shows 0 entries). Widened to uint32. Full trace clean otherwise. |
| 2026-04-08 | rogue_ap_detector | Wired up dead min_rssi setting (value was set in UI but never applied to worker filtering). Added NULL context guard in UART callback to prevent crash on race during scan stop. Updated README. Full trace clean otherwise. |
| 2026-04-09 | ble_scanner      | Added NULL context guard in worker_rx_line for teardown race (same pattern as rogue_ap_detector). Added volatile to scanning field for cross-thread visibility (same fix as nfc_fuzzer). Full trace clean otherwise — buffer sizes correct, mutex usage correct, view lifecycle clean, stack usage safe. |
| 2026-04-10 | rayhunter_client | Added NULL context guard in rh_worker_rx_line for teardown race (same class as rogue_ap_detector/ble_scanner). Added volatile to connected field for cross-thread visibility (UART worker writes, timer daemon reads). Full trace clean otherwise — buffer sizes verified (counters[32] tight but safe), stack usage safe (832/2048 on UART thread), view lifecycle clean, no SD card I/O, settings mutex usage correct, poll timer blocking acceptable (~1.3ms at 115200). Dead defines RH_PORT_MIN/MAX noted but not worth removing. |
| 2026-04-11 | evil_ble         | Added NULL context guard in evil_ble_scanner_rx_cb for teardown race (same class as rogue_ap_detector/ble_scanner/rayhunter_client). Added volatile to scanning field (scanner struct, cross-thread: GUI writes, UART worker reads without mutex) and connected field (UART struct, worker writes, API reads). Full trace clean otherwise — buffer sizes verified (device_labels[64] fits 40-char max, status_buf[256] fits 109-char max, clone_menu_label[48] fits 27-char max), stack usage safe (145/4096 main, 520/2048 UART worker), view lifecycle clean (3 views: submenu×2 + textbox, correct add/remove/free order), back-button handling correct for all views, clone engine extra_beacon config/start/stop correct, scanner mutex usage correct, dedup by MAC correct, djb2 placeholder MAC generation correct, adv payload construction bounded. evil_ble_uart_is_connected declared but never called (same as rogue_ap_detector). No SD card I/O. No cross-app consistency issues. |
| 2026-04-12 | flipperpwn       | **Fixed marauder get_\* race condition** — migrated all 14 callers (5 in wifi_views.c, 9 in payload_engine.c) from unsafe `get_*` to `copy_*` with heap-allocated temporary buffers. Removed dead `get_*` API from marauder.h/marauder.c. Added volatile to wifi_uart.c `connected` field (cross-thread: UART worker writes, GUI reads). Full trace clean — flipperpwn.c view lifecycle correct (17 views), os_detect.c LED restore + CDC bounded, payload_engine.c substitution bounded, wifi_uart.c clean after volatile fix. No new cross-app issues. |
| 2026-04-13 | nfc_fuzzer       | Added furi_assert after malloc in nfc_fuzzer_app_alloc (app struct and results array). Added volatile to worker_running for cross-thread visibility (worker done_cb writes, GUI back_event_cb reads). Fixed misleading LED color comment (blue, not red). Full trace clean otherwise — all snprintf buffers verified (progress buf[64], detail_text[2048], header[128], labels[48]), heap hex buffers avoid stack overflow, dynamic results array with realloc/cap correct, 6 views lifecycle correct, worker 8KB stack safe, listener/poller modes clean, all 11 profile generators bounded, mutex usage correct, SD card logging file lifecycle correct, back-button handling correct, cleanup on all error paths verified. |
| 2026-04-14 | badusb_pro       | Added volatile to worker_running for cross-thread visibility (worker thread writes false on completion, GUI thread reads in app_free and start_script_execution). Full trace clean otherwise — all snprintf buffers verified (info_buf[256], progress[48], led_line[32], os_line[32], current_cmd[64], error_msg[64]), substitute_vars output-bounded, evaluate_condition known edge case (documented 2026-04-03), stack usage safe (1.5KB GUI thread during parsing on 4KB stack, 1.5KB worker thread on 8KB stack), 4 views lifecycle correct (submenu + widget + view + variable_item_list, correct add/remove/free order), USB restore guarded by volatile usb_restored flag, worker thread join-before-reuse correct (Fix #7), token ownership transfer correct (no double-free), back-button handling correct (all events consumed during execution), settings callbacks all bounds-checked. my_strtok_r duplicated in both .c files (harmless). No new cross-app issues — all 13 apps consistent on malloc, fap_author, icon_assets. |
| 2026-04-15 | hid_exfil        | **Fixed macOS cleanup leaving payload commands in zsh history** — `history -p` is csh/tcsh only, no-op in zsh (macOS default since Catalina). Replaced with `unset HISTFILE` so zsh skips writing history on exit. Full trace clean otherwise — all snprintf buffers verified (line[64] fits worst-case 42 chars, assembled_script[8192] fits worst-case ~5350 chars, recv_buffer bounds-checked), 5 views lifecycle correct (widget + submenu + variable_item_list + custom view + textbox, add/remove/free order correct), ViewModelTypeLocking on execution view for thread-safe updates, USB save/restore correct (usb_prev guard from 04-04 fix), worker thread join-before-start/free correct, deferred recv_buffer allocation with OOM handling, LED channel protocol correct (dibit encoding MSB-first, EOT rewind via snapshot), CapsLock pre-flight detection correct, stack usage safe (~280/4096 bytes on worker thread). No new cross-app issues. |
| 2026-04-16 | spi_flash_dump   | **Moved CRC32 computation from timer daemon to worker thread** — `crc32_calc_file` was running in `worker_poll_timer_cb` (FreeRTOS timer daemon context), re-reading the entire dump file from SD card. For 32MB chips this blocked all system timers for 8-32s and froze the UI at ~100%. Now computed in the worker thread right after `chip_read` succeeds, with new `spi_worker_get_crc32`/`has_crc32` accessors. Fixed hex_viewer.h comment (said 8 bytes/row, actually 4) and JEDEC DB count comment (said 30, actually 32). Fixed Settings Back UX — was always returning to WiringGuide even when entered from ChipInfo; now tracks which view opened Settings via file-scope static. Full trace clean otherwise — all snprintf buffers verified (line[80], tmp[32], sz_done/sz_total[16], sz_line[48], crc_str[20], vf_line[48]), uint64 casts on progress bars correct, 6 views lifecycle correct (2 widgets + 2 custom views + hex_viewer + variable_item_list, add/remove/free order correct), worker 4KB stack safe (~306 in chip_read, ~562 in chip_verify), SPI Mode 0 bit-bang correct, 4-byte addressing for >16MB correct, GPIO pins verified (no conflicts), all Storage API returns checked, back-button blocked during active read/verify, settings bounds-checked. No new cross-app issues — malloc/fap_author/icon_assets all consistent. |
| 2026-04-17 | ccid_emulator    | **Fixed APDU monitor auto-scroll not showing newest entries** — `APDU_MON_MAX_VISIBLE=6` but only ~3 entries fit on 128×64 display (each entry = 2 lines × 10px, content area = 52px). With MAX_VISIBLE=6, max scroll offset was too low and auto-scroll/manual scroll could never reach the most recent APDU exchanges. Changed to 3. **Fixed TLV length fields in embedded test_card sample** — MasterFile SELECT: 6F 0x19→0x18, A5 0x0E→0x0D; PSE SELECT: 6F 0x1E→0x1C, A5 0x0C→0x0A. Each overstated content size by 1-2 bytes. Full trace clean otherwise — all snprintf buffers verified (line[80], path[128], header[128], prefix[16], atr_str[107], rules_str[32]), 4 views lifecycle correct (submenu + widget + custom view + variable_item_list, add/remove/free order correct), ViewModelTypeLocking on apdu_monitor, teardown order correct (timer stopped before view freed, mutex freed after timer), ring buffer indexing correct in draw/export/input, USB callback mutex uses 5ms timeout (drops log on contention, never crashes), hex parser bounded, rule parser stack safe (~200B), card loader stack safe (~300B), ccid_handler start/stop ordering correct for SDK and Momentum firmware. No new cross-app issues. |
| 2026-04-18 | rogue_ap_detector | Added volatile to `scanning` (RogueApWorker, cross-thread: GUI writes, timer daemon reads via `rogue_ap_worker_is_scanning`). Added volatile to `connected` (RogueUart, UART worker writes, API reads — consistent with evil_ble/rayhunter/flipperpwn). Removed dead `summary[48]` field and `ROGUE_SCAN_SUMMARY_LEN` from RogueScanModel (declared but never written or read). Full trace clean otherwise — all snprintf buffers verified (ap_line[24], ssid_disp[20], bssid_line[40] fits 39-char max), detection logic O(n²) correct with n≤128, SSID parser beacon-byte stripping correct (known Marauder format limitation), upsert dedup by (ssid,bssid) correct, stale pruning unsigned tick arithmetic correct, 4 views lifecycle correct (submenu + custom view + textbox + variable_item_list, add/remove/free order correct), ViewModelTypeLocking on scan view, teardown order correct (timer→worker→UART→views→dispatcher→objects→results), mutex usage correct (10ms timeout on worker/timer, FuriWaitForever on GUI), RSSI filter applied under mutex, stack safe (~620/2048 on UART worker), settings bounds-checked, back-button stops scan and returns to menu. New cross-app issue: ble_scanner `connected` field missing volatile. |
| 2026-04-19 | flipperpwn       | **Fixed use-after-free race in wifi_views teardown** — `wifi_status_text` and `wifi_status_mutex` were freed before the marauder log callback was deregistered. A late UART line from ESP32 during app exit could invoke `fpwn_wifi_rx_callback` on freed resources. Fix: deregister log callback before freeing string/mutex. Also fixed ble_scanner `connected` volatile (logged 2026-04-18). Full re-trace clean: wifi_uart.c volatile+ISR correct, marauder.c mutex+parsers+copy_* all bounded, flipperpwn.c 17 views lifecycle correct (exec thread management, back-button handling, progress calculations safe), os_detect.c LED probe + CDC detection correct, wifi_views.c 8 views correct (scan timer, save_results heap alloc, scroll calculations bounded), payload_engine.c 3602 lines traced (all command handlers verified: STRING/STRINGLN variable substitution bounded, EXFIL/EXFIL_USB CDC callbacks deregistered before stack exit, INJECT depth guard at 4, FOR/WHILE/REPEAT_BLOCK safety caps, mouse commands int8 clamped, JITTER/RANDOM_INT overflow-safe). Stack usage: exec thread worst case ~6.5KB on 8KB (4-level INJECT with STRING commands). No new cross-app issues. |
| 2026-04-24 | nfc_fuzzer       | **Fixed worker_running race** — `app->worker_running = true` was set after `nfc_fuzzer_worker_start()`, which already starts the thread. If the worker completed before the assignment, done_callback would set it false, then the GUI thread would overwrite to true. Moved assignment before start call. Fixed RATS boundary comment (said 8 PPS/20 total, actually 6 PPS/18 total). Full re-trace clean: all 3401 lines verified — nfc_fuzzer.c (6 views lifecycle correct, draw/input callbacks stack-safe, progress callback heap-allocates hex buffers under mutex, SD card file lifecycle correct, results realloc/cap correct, back-button idempotent stop), nfc_fuzzer_worker.c (listener/poller/NFC-B/FeliCa loops all resource-clean, TimingTracker ring buffer correct, PRNG thread-safe via happens-before, `volatile` on all cross-thread fields, 8KB stack safe), nfc_fuzzer_profiles.c (all 11 profiles × 4 strategies traced — array bounds verified, data_len always ≤255, boundary arrays correctly sized). Stack: main ~200/4096, worker ~360/8192. Heap peak ~52KB at 64 results. No new cross-app issues. |
| 2026-04-25 | badusb_pro       | **Fixed MOUSE_SCROLL value not clamped to int8_t range** — `atoi` result stored directly in `int_value`, executor casts to `int8_t` which silently wraps values outside [-128, 127] (e.g. `MOUSE_SCROLL 200` scrolls as -56, wrong direction). Added `strtol` + `INT8_MIN`/`INT8_MAX` clamping in parser, matching MOUSE_MOVE pattern. Full re-trace clean: all 2900 lines verified — badusb_pro.c (4 views lifecycle correct, USB save/restore with volatile flag, worker join-before-reuse, token ownership transfer no double-free, input_cb consumes Back/Left during execution), ducky_parser.c (ASCII→HID table verified for US layout, key combo parser bounded by words[8]/keycodes[8], all strncpy null-terminated, DEFAULT_DELAY offset logic correct for both variants), script_engine.c (all 25+ token handlers traced, flow control nesting correct, call stack overflow guarded, substitute_vars output-bounded, evaluate_condition known edge case unchanged, consumer key release on all exit paths, RESTART resets pc+call_depth correctly). Stack: GUI ~1260/4096 (parse path), worker ~670/8192 (evaluate_condition path). No new cross-app issues. |
| 2026-04-26 | hid_exfil        | **Fixed macOS cleanup session history leak** — Terminal.app's session-save mechanism (`/etc/zshrc_Apple_Terminal`) writes per-session history to `~/.zsh_sessions/$SHELL_SESSION_FILE` via a `precmd` hook, independently of `HISTFILE`. Previous cleanup removed `~/.zsh_history` and unset `HISTFILE` but missed the session file. Now removes `$SHELL_SESSION_FILE` and unsets both `HISTFILE` and `SHELL_SESSION_FILE`. Added `furi_assert(app->worker)` after `hid_exfil_worker_alloc()` for consistency with cross-app malloc assert pattern. Full re-trace clean: all ~2000 lines verified — hid_exfil.c (5 views lifecycle correct: widget + submenu + variable_item_list + custom view + textbox, add/remove/free order correct, USB save/restore guarded by usb_prev NULL check, worker join-before-free correct), hid_exfil_worker.c (char_to_hid_key US layout correct, CapsLock pre-flight detection correct, phase_inject/sync/receive/cleanup all abort-checked, LED dibit protocol correct with EOT snapshot-rewind, clock timeout 1s correct, save_received_data file lifecycle correct, worker_start sets running before thread_start — no race), hid_exfil_payloads.c (assembled_script[8192] fits largest payload ~1900 chars, PS encoder Add-Type here-string syntax correct for HID typing, bash encoder xdotool/xset correct, all 7×3=21 OS/payload combinations traced). Stack: GUI ~300/4096, worker ~280/4096. No new cross-app issues. |
| 2026-04-27 | subghz_spectrum  | **Fixed worker not stopped when navigating back to band select** — CC1101 kept sweeping while user browsed band menu (unnecessary power drain, radio held). Added view exit callback (`spectrum_view_exit_callback`) to stop worker on view exit. Removed dead `SPECTRUM_DEFAULT_STEP_KHZ` constant. **Closed HAL migration item** — `subghz_devices.h` does not exist in SDK API 87.1; all 7 `furi_hal_subghz_*` functions verified present. Full re-trace clean: all ~860 lines verified — spectrum_worker.c (volatile on `running`, malloc+NULL check on SpectrumData, CC1101 OOK preset registers verified, radio idle+sleep on exit, sweep loop bounded by SPECTRUM_NUM_BINS), subghz_spectrum.c (3 views lifecycle correct: custom view + submenu + variable_item_list, add/remove/free order correct, ViewModelTypeLocking on spectrum view, data_mutex consistent lock ordering with view model lock, CSV logger line[64] fits worst-case 27 chars, header[40] fits worst-case 26 chars, cursor_str[24] fits worst-case 8 chars, waterfall ring buffer indexing correct, dither pattern correct, cursor bounds correct, logging file open/close guarded, back-event stops worker+closes log, band_select stops worker before restart). Stack: worker ~170/4096, GUI no large locals. No new cross-app issues. |
| 2026-04-27 | uart_sniff       | **Fixed ring buffer read returning oldest data instead of newest** — `uart_sniff_worker_read` started from `(ring_head - ring_fill)`, i.e. the oldest byte. Once the ring had >256 bytes, the display showed stale data from the beginning of the capture while the format function's address calculation assumed newest data (producing wrong hex addresses). Fixed to start from `(ring_head - len)` to return the most recent bytes. **Added volatile to `sniffing`** — GUI thread writes, timer daemon reads via `uart_sniff_refresh_cb`. Full re-trace clean: all ~790 lines verified — uart_sniff.c (3 views lifecycle correct: submenu + text_box + variable_item_list, add/remove/free order correct, custom event refresh updates text_box on GUI thread, display format snprintf bounded by remaining-count with goto-done guard, capture_buf[256] matches DISPLAY_BYTES, display_buf[1537] fits 32 lines × 48 chars, address field "%04lX" up to 8 chars but within 48-char budget, all settings bounds-checked), uart_sniff_worker.c (volatile on `running` and `total_rx`, furi_assert after all mallocs, ISR uses stream_buffer with zero timeout, worker drains batch[64] under ring_mutex, ring buffer power-of-2 masking correct, ring_fill capped at RING_SIZE, clear under mutex, serial acquire/init/release ordered correctly, expansion disable/enable bracketed). Stack: worker ~80/1024, GUI no large locals. Timer stopped before app_free. UART capture continues on menu (by design — sniffer shouldn't lose data on Back). |
| 2026-04-28 | flipperpwn       | **Fixed IF_CONNECTED skip not tracking nested IF blocks** — the false-branch skip loop only incremented depth for nested `IF_CONNECTED` but not for regular `IF $VAR == value`. A nested `IF...END_IF` inside `IF_CONNECTED` would cause the skip to stop at the inner `END_IF`, executing commands that should have been skipped when ESP32 is absent. Added `strncmp(st, "IF ", 3)` to the depth check, matching the IF and ELSE skip handlers. Full re-trace clean: all 6 source files verified — flipperpwn.c (17 views lifecycle correct, navigation stack correct, exec thread join-before-start, alloc/free balanced), wifi_uart.c (ISR never blocks, worker lifecycle correct, volatile on connected/running, DMB barrier on callback update), marauder.c (mutex discipline correct, all parsers bounded, copy_* accessors safe, log callback DMB barrier), os_detect.c (LED restore on all paths, CDC lifecycle correct, abort responsive), payload_engine.c (all command handlers re-traced: STRING/STRINGLN bounded, EXFIL/EXFIL_USB CDC lifecycle correct, INJECT depth-guarded at 4, FOR/WHILE/REPEAT_BLOCK safety caps, VAR arithmetic overflow-safe, mouse commands clamped, IF/ELSE/IF_CONNECTED/WHILE skip depth tracking now consistent across all 3 handlers), wifi_views.c (8 views lifecycle correct, teardown order correct: timer→stop→deregister→free, scan timer updates views correctly, status text capped at 4KB, first-connect notification thread-safe). Stack: exec thread ~6.5KB worst case on 8KB. No new cross-app issues. |
| 2026-04-29 | spi_flash_dump   | **Fixed `settings_return_view` forward reference** — file-scope static declared at line 618 but referenced at lines 480 and 557; C requires declaration before use. Moved declaration before `wiring_guide_input_cb`. Full re-trace clean: all 1926 lines verified — spi_worker.c (JEDEC DB 32 entries correct, SPI Mode 0 bit-bang verified, 4-byte addressing correct, GPIO pins PA4/PB3/PA7/PA6 no conflicts, `chip_read` buf[256] and `chip_verify` spi_buf[256]+file_buf[256] stack-safe, CRC in worker thread not timer daemon, all Storage returns checked, volatile on `running`, strncpy NUL-terminated, thread join before reuse), hex_viewer.c (data[4096] heap-allocated via ViewModelTypeLocking, draw snprintf all bounded, scrollbar division-by-zero guarded, no overflow in bar calculations), spi_flash_dump.c (6 views lifecycle correct, all snprintf verified: line[80] tmp[32] sz[16] crc_str[20] dump_path[128], uint64 casts on progress bars, worker_running volatile set before start, timer join blocks microseconds max, back blocked during read/verify, settings bounds-checked, GPIO deinit safe if never initialized). Stack: main ~200/4096, worker ~562/4096. No new cross-app issues. |
| 2026-04-30 | ccid_emulator    | **Fixed APDU monitor auto-scroll overriding manual scroll** — timer callback unconditionally snapped scroll_offset to bottom every 200ms when log_dirty was true, making Up/Down navigation non-functional during active APDU flow. Added `auto_scroll` flag to ApduMonitorModel: disabled on Up press, re-enabled when Down reaches bottom, reset on emulation start. Timer only advances scroll when auto_scroll is true. Full re-trace clean: all ~1600 lines verified — ccid_emulator.c (4 views lifecycle correct: submenu + widget + custom view + variable_item_list, add/remove/free order correct, ViewModelTypeLocking on apdu_monitor, timer stopped after view removal but before view free, lock ordering view_model→log_mutex consistent, no deadlock), card_parser.c (hex/pattern parsers bounded, line[256] safe for max rule lines, rule_count capped at 24, CcidCard alloc+assert+memset, strncpy NUL-terminated, section parser handles all edge cases), ccid_handler.c (ATR capped at 33 bytes, response capped at CCID_MAX_DATA_BLOCK_LEN, log_mutex 5ms timeout on USB thread, log_dirty volatile, start/stop ordering correct for SDK+Momentum, USB restore guarded). All 3 embedded sample cards TLV re-verified correct (test_card MasterFile/PSE/GPO, PIV SELECT/CHUID, JavaCard ISD/GET STATUS). Stack: GUI ~680/4096, USB callback ~70, timer ~100. No new cross-app issues. |
| 2026-05-01 | ble_scanner      | Full re-trace clean — no bugs found. All ~1300 lines verified: ble_uart.c (ISR never blocks, worker thread 2048B stack safe ~857B peak, volatile on connected/running, DMB barrier callback deregistration correct, serial acquisition failure handled gracefully with inert struct, expansion lifecycle correct), ble_scanner_worker.c (worker_rx_line NULL context guard present, volatile on scanning, Marauder scanbt parser bounded — find_mac validates hex+colons, name_hash_mac FNV-1a output fits MAC_LEN, name_is_airtag lower[32] bounded, find_or_insert capped at 64 with sentinel check, worker_log line[96] fits worst-case 55 chars, worker_open_log path[64] fits 48-char path, oui_is_apple 8-char strncmp correct, RSSI int8_t cast safe for validated [-120,0] range), ble_scanner.c (3 views lifecycle correct: submenu + custom view + variable_item_list, ViewModelTypeLocking on scan view, add/remove/free order correct, sort_devices insertion sort correct for 3 modes, refresh timer heap-allocates 3584B work buffer avoiding timer daemon stack overflow, scroll indicator bar_y+bar_h max=63 within 64px, division-by-zero guarded, lock ordering results_mutex→view_model consistent — no deadlock, teardown order correct: timer→worker→UART→results→views→dispatcher→records, back-button stops scan and returns to menu, settings bounds-checked). Stack: main ~500/4096, UART worker ~857/2048, timer daemon ~145/1024. No new cross-app issues. |
| 2026-05-02 | rayhunter_client | Full re-trace clean — no bugs found. All 1190 lines verified across 3 source files: rayhunter_uart.c (ISR zero-timeout push correct, worker 2048B stack safe ~752B peak including callback, line_buf[512] overflow guard correct, connected detection requires RH protocol markers, serial failure returns inert struct, teardown order correct: running→join→free→async_rx_stop→deinit→release→stream_free, expansion lifecycle correct), rayhunter_worker.c (threat keyword table ordered high→low with JSON-quoted strings, alert/status keyword search correct, rh_worker_rx_line NULL guard + line guard correct, all snprintf bounded — alert_msg[128] fits 17-char max keyword, status_buf[64] fits 17-char max string, model update under ViewModelTypeLocking, threat ratchet-up correct, high-threat notification de-dup correct — last_notified_threat single-thread access only, rh_worker_poll resets threat on poll window, UART send ~1.3ms on timer daemon acceptable), rayhunter.c (3 views lifecycle correct: View + VariableItemList + Widget, add/remove/free order correct, ViewModelTypeLocking on main view, navigation_cb exit-on-back + return-to-main correct, custom_event_cb handles both events, draw_cb all buffers verified — status_row[72] counters[32] exact-fit footer[48] msg_buf[26], threat label centering clamped, settings callbacks all index-clamped + mutex-protected + model-synced, port_str[8] secs_str[8] bounded, timer restart thread-safe, teardown order correct: timer→worker→remove_views→free_dispatcher→free_views→free_uart→free_mutex→close_records). Stack: GUI ~218/6144, UART worker ~752/2048, timer daemon ~86/configDefault. No new cross-app issues. |
| 2026-05-03 | evil_ble         | **Removed dead `devices[32]` and `device_count` fields from EvilBleApp** — scanner manages its own array via mutex-protected accessors; the duplicate fields in the app struct were never read or written, wasting ~2852 bytes of heap. Full re-trace clean otherwise. |
| 2026-05-04 | subghz_jammer    | Added `furi_assert(worker)` in `jammer_worker_alloc` and `furi_assert(app->worker)` in `jammer_app_alloc` — worker malloc was the last unchecked allocation in the repo. Full re-trace clean: all ~730 lines verified — worker thread CC1101 init error paths correct (hw_error under mutex, deinit, record close), scan loop RSSI→window→threshold→consecutive→worst-freq logic correct with hysteresis, mutex 50ms timeout skips gracefully, short-circuit protects worst-freq index comparison, notifications fired outside mutex, `running` is `volatile bool`, `alert_cb` set-before-start (happens-before), main view draw all buffers verified (rssi_str[8] fits [-127,0] as 4 chars, alert_str[32] fits 24-char max), timer callback ~58B on daemon stack, settings callbacks use SDK-clamped indices, 2 views lifecycle correct (View + VariableItemList, add/remove/free order correct), teardown order correct (timer→worker→views→dispatcher→state→records), back-button exits via VIEW_NONE. Stack: main ~200/4096, worker ~100/4096, timer ~58/1024. No new cross-app issues. |
| 2026-05-08 | flipperpwn       | **Fixed WiFi text input callback cleared by `text_input_reset`** — both the AP scan OK handler (join password entry) and Evil Portal SSID entry called `text_input_reset` which nulls the result callback, making the OK button on the text input silently dead. Added `text_input_set_result_callback` after each reset. Full re-trace clean otherwise: all 8237 lines verified across 9 files — wifi_uart.c (ISR non-blocking, worker 2048B stack safe, volatile on connected/running, DMB on callback, teardown order correct), os_detect.c (LED restore on all paths, CDC abort-responsive, case-insensitive matching, HID readiness probe), marauder.c (mutex discipline correct, all 5 parsers bounded, copy_* accessors safe, log callback fires outside mutex, DMB on set_log_callback), flipperpwn.c (17 views lifecycle correct, exec thread join-before-start, back-stack navigation complete for all 17 views, USB save/restore guarded), wifi_views.c (8 views lifecycle correct, timer→stop→deregister→free teardown, copy_* in timer callback, scroll indicators division-by-zero guarded, first-connect notification thread-safe, status text 4KB cap), payload_engine.c (all 40+ command handlers traced: STRING/STRINGLN var substitution bounded at 512B, EXFIL/EXFIL_USB CDC lifecycle correct — callbacks before switch/deregister before restore, INJECT depth guard at 4, FOR/WHILE/REPEAT_BLOCK safety caps 10000/1000/100, mouse/scroll int8 clamped, IF/ELSE/IF_CONNECTED skip depth tracking consistent across all handlers, VAR arithmetic int64 overflow-safe with div-by-zero guard, JITTER/RANDOM_INT range+1 overflow guarded, TYPE_FILE abort-checked, WiFi commands all check ESP32 connection and use copy_*). Stack: exec thread ~5.2KB worst case on 8KB (4-level INJECT with STRING). No new cross-app issues. |
| 2026-05-06 | nfc_fuzzer       | Added `furi_assert(app->worker)` after `nfc_fuzzer_worker_alloc()` — worker_alloc returns NULL on OOM (unlike all other apps that furi_assert internally), but the caller in nfc_fuzzer_app_alloc didn't check. Full re-trace clean: all ~3350 lines verified — nfc_fuzzer.c (6 views lifecycle correct: 3 submenus + custom View + TextBox + VariableItemList, add/remove/free order correct, ViewModelTypeLocking on fuzz_run view, progress callback mutex-protected with heap-allocated hex buffers for SD card logging, results realloc/cap bounded at 64×520B, worker_running set before worker_start, back-button idempotent stop), nfc_fuzzer_worker.c (listener mode: per-iteration listener restart for UID/ATQA correct, BitBuffers heap-allocated, timeout anomaly deferred until baseline established, response copy capped at MAX_PAYLOAD_LEN; poller mode: trx called from callback per SDK 1.4.3 requirement, PollerFuzzCtx ~136B on 8KB stack; NFC-B/FeliCa: per-iteration listener restart with fuzzed PUPI/IDm correct, cleanup complete on all paths; volatile on running/stop_requested, thread join-before-reuse correct), nfc_fuzzer_profiles.c (xorshift32 PRNG single-threaded via happens-before, all 11 profiles × 4 strategies traced — UID max 10B, ATQA/SAK 3B, Frame max 65B, NTAG max 6B, ISO15693 max 26B, Reader max 8B, MIFARE Auth 8B, MIFARE R/W max 18B, RATS max 49B, NFC-B 4B, FeliCa 8B — all ≤255, boundary arrays correctly sized, total counts match). Stack: main ~200/4096, worker ~400/8192. Heap peak ~40KB. No new cross-app issues. |
| 2026-05-15 | badusb_pro       | Full re-trace clean — no bugs found. All 2900 lines verified across 3 source files: badusb_pro.c (4 views lifecycle correct: Submenu + Widget + custom View + VariableItemList, add/remove/free order correct, ViewModelTypeLocking on execution view, worker join-before-reuse correct, USB save/restore with volatile usb_restored flag, USB enumeration wait up to 6.5s, all snprintf verified — info_buf[256] fits 117-char max, progress[48] fits 23, led_line[32] fits 20, os_line[32] fits 21, current_cmd[64] fits all format paths, settings callbacks all index-bounded), ducky_parser.c (ASCII→HID table verified 95 entries for US layout, parse_key_combo words[8]/keycodes[8] bounded, all token handlers use strncpy(sizeof-1) from memset-zeroed token, DEFAULTDELAY offset logic correct for both variants, MOUSE_SCROLL strtol+INT8 clamp present, file parser stream/string lifecycle correct), script_engine.c (substitute_vars output-bounded with truncation warning, evaluate_condition known edge case unchanged, type_char table lookup bounds-checked, press_key_combo OR-merge correct, all flow control depth-tracked — find_else_or_endif/find_endif/find_end_while/find_matching_while handle nesting, CALL depth guarded at MAX_STACK=32, RESTART resets pc+call_depth preserving vars, do_os_detect CapsLock toggle+timing+restore correct, do_led_wait polls with state check, consumer key lookup+hex fallback correct, completion releases all keys+consumer keys). Stack: GUI ~1160/4096 (parse path), worker ~700/8192 (evaluate_condition path). All prior fixes verified present: MOUSE_SCROLL clamp (2026-04-25), volatile worker_running (2026-04-14), malloc NULL checks (2026-04-03). No new cross-app issues. |
| 2026-05-17 | rogue_ap_detector | Full re-trace clean — no bugs found. All ~1400 lines verified across 3 source files + 3 headers: rogue_uart.c (ISR zero-timeout push correct, worker 2048B stack safe ~700B peak with line_buf[512], volatile on connected/running, [BUF/ filter correct, teardown order correct: running→join→free→async_rx_stop→deinit→release→stream_free, serial failure returns inert struct, expansion lifecycle correct), rogue_ap_worker.c (is_valid_bssid validates all 17 positions, parse_marauder_ap handles both scanap and list-a formats with correct buffer bounds, beacon-byte stripping correct for ≤3-digit tokens, rogue_detect O(n²) correct with min/max RSSI in single inner loop, rogue_prune_stale unsigned tick arithmetic correct, rogue_uart_line_cb NULL ctx guard + channel/RSSI sanity + 10ms mutex timeout + upsert by (ssid,bssid) + detect after every update), rogue_ap.c (4 views lifecycle correct: Submenu + View + TextBox + VariableItemList, add/remove/free order correct, ViewModelTypeLocking on scan view, refresh timer 10ms mutex timeout + snapshot copy + custom event notification, input_callback OK toggles scan/stop, rebuild_results O(n²) under mutex correct, nav_callback stops scan on Back, settings 1-byte atomic write to min_rssi, teardown order correct: timer→worker→UART→view_removal→dispatcher→objects→results→records). Stack: main ~200/4096, UART worker ~700/2048, timer ~60/1024. Prior fixes verified present: volatile scanning (2026-04-18), volatile connected (2026-04-18), NULL ctx guard (2026-04-08), min_rssi wiring (2026-04-08), dead summary field removal (2026-04-18). Known characteristics: rogue_uart_is_connected() declared but never called (harmless), SSID trailing-digit stripping inherent Marauder limitation, detection O(n²) with n≤128 completes in <1ms, rebuild_results holds mutex for full duration (drops UART lines, acceptable for rare user action). No SD card I/O. No new cross-app issues. |
| 2026-05-19 | hid_exfil        | **Fixed Linux cleanup not handling zsh** — `history -c && history -w` is bash-only; on zsh (increasingly common on Linux), payload commands were saved to `~/.zsh_history`. Replaced with `rm -f ~/.bash_history ~/.zsh_history` + `unset HISTFILE` (same pattern as Mac cleanup fixed 2026-04-15). **Fixed deprecated `Get-WmiObject`** in Windows sysinfo payload — replaced with `Get-CimInstance` (same fix as flipperpwn `fake_login` 2026-05-15). **Fixed `-Werror=comment` build failure** — glob `*.history` in comment contained `/*` triggering nested comment warning; rewrote as `(*.history)`. Full re-trace clean otherwise: all ~2000 lines verified across 3 source files + 3 headers — hid_exfil.c (5 views lifecycle correct: Widget + Submenu + VariableItemList + custom View + TextBox, add/remove/free order correct, ViewModelTypeLocking on execution view, worker_callback cross-thread update safe, USB save/restore guarded by usb_prev NULL check — first-run-only save correct, config callbacks all bounds-checked, teardown order correct: worker→USB_restore→remove_views→free_views→free_dispatcher→close_records→free_data), hid_exfil_worker.c (char_to_hid_key US layout 95 chars correct with space fallback, ensure_capslock_off pre-flight correct, type_string abort+pause responsive on every char, phase_inject terminal-open per-OS correct, phase_sync retry-on-timeout with re-send correct, phase_receive LED dibit protocol correct — MSB-first 4 dibits/byte, EOT snapshot-rewind discards spurious bytes from 3 all-LED toggles, clock timeout 1s with data→end/nodata→reset, phase_cleanup per-OS correct including Mac session history, save_received_data file lifecycle correct with timestamped path, pre-flight CapsLock LED test correct with restore, worker thread state init complete, running set before thread_start — no race, thread join-before-reuse in worker_start), hid_exfil_payloads.c (ps_encoder_func Add-Type P/Invoke correct — keybd_event+GetKeyState, dibit encoding MSB-first, EOT 3-toggle with state restore; bash_encoder_func xdotool+xset correct — same protocol, EOT correct; all 7×3=21 OS/payload combinations traced — assembled_script[8192] fits largest assembly ~2400 chars, snprintf bounded). Stack: GUI ~300/4096, worker ~130/4096. No new cross-app issues. |
| 2026-05-20 | spi_flash_dump   | Full re-trace clean — no bugs found. All 1926 lines verified across 3 source files + 2 headers: spi_worker.c (JEDEC DB 32 entries correct, SPI Mode 0 bit-bang verified, 4-byte addressing for >16MB correct, GPIO pins PA4/PB3/PA7/PA6 no conflicts, chip_read buf[256] + chip_verify spi_buf[256]+file_buf[256] stack-safe, CRC32 in worker thread, all Storage returns checked, volatile on running, strncpy NUL-terminated, thread join-before-start), hex_viewer.c (data[4096] heap via ViewModelTypeLocking, draw bounds correct, scrollbar division-by-zero guarded, load_file storage lifecycle correct), spi_flash_dump.c (6 views lifecycle correct: 2 Widgets + 2 custom Views + HexViewer + VariableItemList, ViewModelTypeLocking on both progress views, all snprintf verified, uint64 casts on progress+speed, worker_running volatile set before start, timer stopped before app_free, GPIO deinit safe, settings index-clamped, back blocked during read/verify, cleanup timer→worker→GPIO→views→dispatcher→records, cross-thread safe via ViewModelTypeLocking + volatile + atomic uint32 + thread join). Stack: main ~200/4096, worker ~562/4096, timer ~60/1024. No new cross-app issues. |
| 2026-07-19 | ccid_emulator    | **Fixed 32-byte APDU truncation (issue #59)** — split `CCID_EMU_MAX_APDU_LEN` into `MAX_CMD_LEN` (64) / `MAX_RESP_LEN` (128); hex parsers now signal overflow (return 0) instead of returning a partial count, and `parse_rule_line` skips + logs over-length rules instead of storing them truncated. Enlarged rule-line read buffer. Added MIT `LICENSE` + README reference (issue #12). |
| 2026-07-19 | PR backlog       | **Consolidated real unmerged fixes from the draft-PR backlog** and confirmed `main` did not build against the OFW SDK the CI targets. Real build breaks fixed: `variable_item_list_set_header()` removed from ble_scanner/rayhunter_client/uart_sniff (absent in OFW — `-Werror=implicit-function-declaration`, from PR #65), and flipperpwn `fpwn_wifi_password_done` used-before-declared (forward decl added). Also folded: flipperpwn teardown race (#44), rayhunter UAF in `rh_app_free` (#58), rogue_ap re-detect-before-bail (#58), spi_flash_dump worker_state start-up race (#58), `build_all.sh` `set -e` post-increment abort (#26). **Corrected two wrong build claims from an earlier pass** by checking the OFW headers directly: `nfc_listener_tx()` takes `Nfc*` (per `lib/nfc/nfc.h`), so nfc_fuzzer's pristine call was left unchanged (an earlier pass wrongly "fixed" it to `NfcListener*`); and `variable_item_list_set_header()` is genuinely absent, so it was removed rather than kept. Closed stale/superseded drafts. |

## Open Items

### Cross-App Issues

- **Issue #6 — Empty `images/` directories**: RESOLVED 2026-03-31. Removed `fap_icon_assets="images"` from all 7 apps (badusb_pro, ccid_emulator, flipperpwn, hid_exfil, nfc_fuzzer, spi_flash_dump, subghz_spectrum). No app uses compiled icon assets. GitHub issue can be closed.
- **Issue #4 — CCID VID/PID customization**: CLOSED 2026-04-13 (not_planned). SDK does not support custom USB descriptors for CCID. Dead preset UI removed (commit 7e63dca). Presets array retained for future SDK support.
- **Issue #3 — CI lint/format check**: CLOSED 2026-04-13 (completed). Added `.github/workflows/build.yml` (2026-04-03) with build + lint matrix for all 13 FAPs. Added `build_all.sh` for local use.
- **SD card paths**: 8 of 13 apps use `/ext/<app_name>/` instead of the conventional `/ext/apps_data/<app_name>/`: `badusb_pro` (`/ext/badusb_pro/`), `ble_scanner` (`/ext/ble_scanner/`), `ccid_emulator` (`/ext/ccid_emulator/`), `flipperpwn` (`/ext/flipperpwn/`), `hid_exfil` (`/ext/hid_exfil/`), `nfc_fuzzer` (`/ext/nfc_fuzzer/`), `spi_flash_dump` (`/ext/spi_dumps/`), `subghz_spectrum` (`/ext/subghz_spectrum/`). The remaining 5 apps (evil_ble, rayhunter_client, rogue_ap_detector, subghz_jammer, uart_sniff) don't use SD card storage. Should migrate all 8 to `/ext/apps_data/` in a coordinated session to avoid polluting the SD card root.
- **malloc NULL checks**: RESOLVED 2026-05-06. All 13 FAPs now have explicit malloc NULL checks on app struct, state struct, and worker struct allocations. nfc_fuzzer caller-side worker assert was the last gap (fixed 2026-05-06) — `nfc_fuzzer_worker_alloc` uniquely returns NULL on OOM instead of asserting internally, so the caller needed an explicit `furi_assert(app->worker)`. Prior fixes: subghz_jammer worker alloc (2026-05-04), subghz_spectrum + subghz_jammer app/state (2026-04-13), nfc_fuzzer app+results (2026-04-13), ccid_emulator (2026-04-06), spi_flash_dump (2026-04-05), hid_exfil (2026-04-04).
- **Issue #12 — License question**: RESOLVED 2026-07-19. Added an MIT `LICENSE` file (copyright barkandbite, 2026) and updated the README License section to reference it. MIT chosen as a permissive default fitting this "contributions welcome" utility collection; the copyright holder can relicense later if desired. GitHub issue can be closed.

- **Issue #62 — OFW moving CCID out of firmware HAL**: OPEN (2026-07-07). Reported by RogueMaster re: flipperdevices/flipperzero-firmware#4407. Official firmware is removing the CCID USB stack from the HAL and requiring apps to bundle it. Migration (per RogueMaster's reference commit): `#include <furi_hal_usb_ccid.h>` → `#include "ccid_usb.h"`; `furi_hal_usb_ccid_set_callbacks/insert_smartcard/remove_smartcard()` → `ccid_usb_set_callbacks/insert_smartcard/remove_smartcard()`; USB interface `usb_ccid` → `ccid_usb_interface`; new `FuriHalUsbCcidConfig {vid,pid,manuf,product}` enables the VID/PID customization that issue #4 previously deferred. **Architecturally significant + not a blind change**: apps must vendor `ccid_usb.c/.h`, and an unconditional switch breaks the app on firmwares that still ship CCID in the HAL (Momentum, current Unleashed). Needs a firmware-target decision and conditional compilation, and can't be build-tested without the new SDK. Deferred out of the 2026-07-19 maintenance PR; tracked here for a dedicated session. Bonus: this migration would also close issue #4 (CCID VID/PID customization) since `FuriHalUsbCcidConfig` exposes vid/pid.

### Per-App Items

- **flipperpwn**: Reviewed 2026-05-08. **Fixed WiFi text input callback cleared by `text_input_reset`** — `text_input_reset` nulls the result callback and text buffer; the AP scan OK handler (join password) and Evil Portal SSID entry both called it without re-registering the callback. OK button on the text input was silently dead. Added `text_input_set_result_callback` after each reset. Prior fixes: IF_CONNECTED skip depth (2026-04-28), use-after-free in wifi_views teardown (2026-04-19), marauder `get_*` race (2026-04-12), volatile on `wifi_uart.c:connected` (2026-04-12), EXFIL_USB COM port filtering (2026-04-01). Full re-trace clean: all 8237 lines verified across 9 files — wifi_uart.c (ISR/worker/teardown), os_detect.c (LED+CDC), marauder.c (mutex+parsers+copy_*), flipperpwn.c (17 views lifecycle), wifi_views.c (8 views+timer+teardown), payload_engine.c (40+ commands traced). Stack: exec thread ~5.2KB worst case on 8KB (4-level INJECT with STRING). No open items.
- **ccid_emulator**: Reviewed 2026-07-19 (issue #59). **Fixed APDU command/response rules silently truncated at 32 bytes** — `CCID_EMU_MAX_APDU_LEN` (32) capped both `command[]` and `response[]`; `parse_hex_string`/`parse_hex_pattern` stopped at the cap and returned a *non-zero* count, so `parse_rule_line` stored a truncated-but-valid-looking rule (`rule_count++`), silently dropping response bytes including the trailing status word (e.g. `90 00`). The shipped `ccid_emulator_sample_cards/piv_emulator.ccid` 62-byte CHUID response hit exactly this. Fix: split the single bound into `CCID_EMU_MAX_CMD_LEN` (64) and `CCID_EMU_MAX_RESP_LEN` (128); both hex parsers now return 0 on overflow instead of a partial count; `parse_rule_line` skips + `FURI_LOG_W`s any over-length rule rather than storing it truncated; the rule-line read buffer was enlarged to fit a full command+response line. 128 (not a full 256-byte data block) chosen because the 4 KB app stack + rule-line buffer make 256 impractical, and 128 covers every response the sample cards and typical EMV/PIV cards emit. Note: the 2026-03-30 sample-card audit called these files "clean" because the *embedded* built-in samples all sit ≤32 bytes — the file-loaded 62-byte case was the gap. Prior fix on this app: **Fixed APDU monitor auto-scroll overriding manual scroll** — timer callback unconditionally snapped scroll_offset to bottom every 200ms during active APDU flow, making Up/Down navigation non-functional. Added `auto_scroll` flag to ApduMonitorModel. Prior fixes: APDU monitor MAX_VISIBLE 6→3 (2026-04-17), TLV lengths in test_card sample (2026-04-17), log_count uint16→uint32 (2026-04-06), malloc NULL check (2026-04-06). Full re-trace clean — all buffers verified, 4 views lifecycle correct, ViewModelTypeLocking on apdu_monitor, lock ordering view_model→log_mutex consistent, timer+teardown order correct, USB callback 5ms mutex timeout, hex/pattern/card parsers bounded, handler start/stop ordered for SDK+Momentum, all 3 sample card TLV structures re-verified correct. SD card path uses `/ext/ccid_emulator/` (existing cross-app issue). discover_card_files second dir_open unchecked (no crash, same as 04-17). No open items.
- **hid_exfil**: Reviewed 2026-05-19. **Fixed Linux cleanup not handling zsh** — `history -c && history -w` is bash-only; replaced with `rm -f ~/.bash_history ~/.zsh_history` + `unset HISTFILE` matching Mac cleanup pattern. **Fixed deprecated `Get-WmiObject`** in sysinfo payload → `Get-CimInstance`. **Fixed `-Werror=comment` build failure** from `/*.history` glob in comment. Prior fixes: macOS session history leak (2026-04-26), macOS `history -p` → `unset HISTFILE` (2026-04-15), USB config loss on re-run (2026-04-04), malloc NULL check (2026-04-04), furi_assert(app->worker) (2026-04-26). Full re-trace clean: all ~2000 lines verified across 3 source files — hid_exfil.c (5 views lifecycle correct, ViewModelTypeLocking on execution view, USB save/restore guarded, teardown order correct), hid_exfil_worker.c (char_to_hid_key US layout correct, LED dibit protocol correct with EOT snapshot-rewind, pre-flight CapsLock test correct, per-OS cleanup all correct, save_received_data file lifecycle correct, running set-before-start — no race), hid_exfil_payloads.c (all 21 OS/payload combos traced, assembled_script[8192] fits ~2400 char max). Stack: GUI ~300/4096, worker ~130/4096. Known UX issues: GUI thread blocks ~6.5s during USB HID setup; `assembled_script[8192]` static global (only worker-thread access, no race); Linux payloads require X11 + xdotool/xset (Wayland won't work); `storage_file_write` return unchecked in `save_received_data` (data still viewable in memory); macOS `airport -I` deprecated (still functional as of macOS 15). SD card path uses `/ext/hid_exfil/` (existing cross-app issue). No open items.
- **badusb_pro**: Reviewed 2026-05-15. Full re-trace clean — no bugs found. All 2900 lines verified across 3 source files: badusb_pro.c (4 views lifecycle correct, ViewModelTypeLocking on execution view, worker join-before-reuse correct, USB save/restore with volatile usb_restored flag, all snprintf buffers verified, settings callbacks index-bounded), ducky_parser.c (ASCII→HID table 95 entries correct, parse_key_combo words[8]/keycodes[8] bounded, all strncpy from memset-zeroed token, MOUSE_SCROLL strtol+INT8 clamp present), script_engine.c (substitute_vars output-bounded, all flow control depth-tracked, CALL depth guarded at 32, RESTART resets pc+call_depth, do_os_detect correct, completion releases all keys). Stack: GUI ~1160/4096, worker ~700/8192. Prior fixes: MOUSE_SCROLL clamp (2026-04-25), volatile worker_running (2026-04-14), malloc NULL check (2026-04-03), dead code removal (2026-04-03). Known edge cases: REPEAT doesn't support mouse/consumer/LED/VAR tokens (feature gap, not crash); evaluate_condition confused by `==`/`!=` in substituted variable values; USB enumeration blocks GUI thread up to 6.5s (UX issue, same as hid_exfil); my_strtok_r duplicated in both .c files (harmless). SD card path uses `/ext/badusb_pro/` (existing cross-app issue). No open items.
- **nfc_fuzzer**: Reviewed 2026-05-06. **Added `furi_assert(app->worker)` after `nfc_fuzzer_worker_alloc()`** — worker_alloc returns NULL on OOM (unique among all 13 apps — all others furi_assert internally), but caller didn't check. Prior fixes: worker_running race (2026-04-24), RATS boundary comment (2026-04-24), furi_assert after malloc for app+results (2026-04-13), volatile on worker_running (2026-04-13), LED color comment (2026-04-13), log truncation + progress overflow + volatile thread fields (2026-04-02). Full re-trace clean: all ~3350 lines verified — 6 views lifecycle correct (ViewModelTypeLocking on fuzz_run), progress callback mutex-protected with heap hex buffers, results realloc/cap bounded at 64×520B, worker_running set-before-start, listener/poller/NFC-B/FeliCa loops all resource-clean, TimingTracker ring buffer correct, PRNG single-threaded, 11 profiles × 4 strategies all bounded (max data_len: Frame 65B, RATS 49B, MIFARE R/W 18B — all ≤255). Stack: main ~200/4096, worker ~400/8192. Heap peak ~40KB. No open items.
- **subghz_jammer**: Reviewed 2026-05-04. Added `furi_assert` after worker malloc (worker_alloc and app_alloc). Full re-trace clean: CC1101 device init error handling correct, scan loop logic correct (RSSI window, threshold comparison with hysteresis, consecutive counter freezes on downgrade/resets on Ok, worst-freq short-circuit safe), mutex discipline correct (50ms worker timeout, FuriWaitForever in settings), 2 views lifecycle correct, timer ~58B on daemon stack, volatile on `running`, no SD card I/O, no resource leaks on any path. Stack: main ~200/4096, worker ~100/4096. No open items.
- **uart_sniff**: Reviewed 2026-04-27. **Fixed ring buffer read returning oldest data instead of newest** — `uart_sniff_worker_read` started from `(ring_head - ring_fill)` instead of `(ring_head - len)`. Display showed stale capture data once ring had >256 bytes. Added volatile to `sniffing` (GUI thread writes, timer daemon reads). Full re-trace clean — all buffers verified (display_buf[1537] fits 32×48 worst case, batch[64] in worker, capture_buf[256] matches display bytes), 3 views lifecycle correct, ring buffer masking correct, ISR non-blocking, serial lifecycle correct, expansion disable/enable correct. UART capture continues on menu (by design). No SD card I/O. No open items.
- **subghz_spectrum**: Reviewed 2026-04-27. **Fixed worker not stopped on back navigation** — added view exit callback to stop CC1101 sweep when leaving spectrum view. Removed dead `SPECTRUM_DEFAULT_STEP_KHZ` constant. **HAL API migration**: RESOLVED — `subghz_devices.h` does not exist in SDK API 87.1; `furi_hal_subghz_*` is the correct API for this target. Full re-trace clean — all buffers verified, 3 views lifecycle correct, mutex discipline correct, waterfall/dither/cursor all bounded, CC1101 preset verified, radio cleanup correct, back-button handling correct, CSV logging correct. Known UX: wider bands (433/868/915) clip at 128 bins (partial band displayed, no user feedback); peak_hold/view_mode settings and runtime state can desync (Up/Down buttons modify runtime only); CSV logging does 128 individual `storage_file_write` calls per sweep (performance, not correctness). SD card path uses `/ext/subghz_spectrum/` (existing cross-app issue). No open items.
- **spi_flash_dump**: Reviewed 2026-05-20. Full re-trace clean — no bugs found. All 1926 lines verified across 3 source files + 2 headers: spi_worker.c (JEDEC DB 32 entries correct, SPI Mode 0 bit-bang verified, 4-byte addressing correct, GPIO pins PA4/PB3/PA7/PA6 no conflicts, chip_detect fallback size bounded, chip_read buf[256] + chip_verify spi_buf[256]+file_buf[256] stack-safe, CRC32 in worker thread, all Storage returns checked, file lifecycle correct on all paths, volatile on running, strncpy NUL-terminated, thread join-before-start), hex_viewer.c (data[4096] heap via ViewModelTypeLocking, all draw bounds correct, scrollbar division-by-zero guarded, input scroll bounded, load_file storage lifecycle correct), spi_flash_dump.c (6 views lifecycle correct: 2 Widgets + 2 custom Views + HexViewer + VariableItemList, ViewModelTypeLocking on both progress views, all snprintf verified — line[80] tmp[32] sz[16] crc_str[20] dump_path[128] all bounded, uint64 casts on progress+speed, worker_running volatile set before start, timer stopped before app_free, GPIO deinit safe, settings index-clamped, back blocked during read/verify, cleanup timer→worker→GPIO→views→dispatcher→records, cross-thread safe via ViewModelTypeLocking + volatile + atomic uint32 + thread join). Stack: main ~200/4096, worker ~562/4096, timer ~60/1024. Prior fixes verified: CRC32 in worker (2026-04-16), Settings Back UX (2026-04-16/04-29), progress bar uint64 casts (2026-04-05), malloc NULL checks (2026-04-05). SD card path uses `/ext/spi_dumps/` (existing cross-app issue). No open items.
- **rogue_ap_detector**: Reviewed 2026-05-17. Full re-trace clean — no bugs found. All ~1400 lines verified across 3 source files + 3 headers: rogue_uart.c (ISR zero-timeout push, worker 2048B stack safe ~700B, volatile on connected/running, [BUF/ filter, teardown order correct, serial failure inert struct, expansion lifecycle correct), rogue_ap_worker.c (is_valid_bssid validates all 17 positions, parse_marauder_ap scanap+list-a formats bounded, beacon-byte stripping correct, rogue_detect O(n²) min/max in single loop, prune_stale unsigned tick wrap correct, line_cb NULL guard + 10ms mutex + upsert + detect), rogue_ap.c (4 views lifecycle correct, ViewModelTypeLocking on scan view, refresh timer snapshot copy, input OK toggles scan, rebuild_results O(n²) under mutex, nav stops scan on Back, settings atomic min_rssi, teardown timer→worker→UART→views→dispatcher→objects→results→records). Stack: main ~200/4096, UART worker ~700/2048, timer ~60/1024. Prior fixes verified: volatile scanning+connected (2026-04-18), NULL ctx guard (2026-04-08), min_rssi wiring (2026-04-08), dead summary removal (2026-04-18). Known characteristics: rogue_uart_is_connected() declared but never called (harmless), SSID trailing-digit stripping inherent Marauder limitation. No SD card I/O. No open items.
- **ble_scanner**: Reviewed 2026-05-01. Full re-trace clean — no bugs found. All ~1300 lines verified: ble_uart.c (ISR→stream_buffer→worker→callback pipeline correct, DMB barrier deregistration correct, serial failure returns inert struct, expansion lifecycle correct), ble_scanner_worker.c (scanbt parser bounded, find_mac validates all hex/colon positions, name_hash_mac FNV-1a output fits MAC_LEN, find_or_insert sentinel-checked, worker_log worst-case 55/96 chars, open_log path 48/64 chars, oui_is_apple 8-char compare correct, AirTag heuristic correct for both MAC and named devices), ble_scanner.c (3 views lifecycle correct, ViewModelTypeLocking on scan view, insertion sort correct, refresh timer heap-allocates work buffer, scroll indicator bounded, lock ordering results_mutex→view_model — no deadlock, teardown timer→worker→UART→results→views correct). Stack: main ~500/4096, UART worker ~857/2048, timer ~145/1024. Prior fixes all verified present: NULL context guard (2026-04-09), volatile scanning (2026-04-09), volatile connected (2026-04-19). Known characteristics: refresh timer runs continuously (same as rogue_ap_detector — negligible), `ble_uart_is_connected()` correctly used from refresh timer, SD card path `/ext/ble_scanner/` (existing cross-app issue). No open items.
- **rayhunter_client**: Reviewed 2026-05-02. Full re-trace clean — no bugs found. All 1190 lines verified: rayhunter_uart.c (ISR zero-timeout correct, worker 2048B stack safe ~752B peak, line_buf[512] overflow guard correct, connected detection requires RH protocol markers preventing false positives, serial failure returns inert struct, teardown: running→join→free→async_rx_stop→deinit→release→stream_free correct, rx_callback/ctx not volatile but safe due to compiler barrier from furi_stream_buffer_receive), rayhunter_worker.c (threat keywords JSON-quoted and priority-ordered, alert/status keyword search bounded, model update under ViewModelTypeLocking, threat ratchet + notification de-dup correct, rh_worker_poll ~1.3ms on timer daemon acceptable), rayhunter.c (3 views lifecycle correct: View + VariableItemList + Widget, ViewModelTypeLocking on main view, draw buffers all verified — status_row[72] counters[32] exact-fit footer[48] msg_buf[26], settings index-clamped + mutex-protected + model-synced, teardown timer→worker→views→dispatcher→uart→mutex→records correct). Prior fixes: NULL context guard (2026-04-10), volatile connected (2026-04-10). Known characteristics: config_mutex defensive (only GUI thread writes), RH_PORT_MIN/MAX dead defines (harmless), counters[32] exact fit (snprintf-bounded), view_dispatcher_stop redundant with return false in navigation_cb (harmless), rh_worker_start return value unused (harmless). No SD card I/O. No open items.
- **evil_ble**: Reviewed 2026-05-03. **Removed dead `devices[32]` and `device_count` fields from EvilBleApp** — scanner manages its own internal device array via mutex-protected accessors (evil_ble_scanner_get_device/get_count); the duplicate fields in the app struct were declared but never read or written anywhere in the codebase, wasting ~2852 bytes of heap on a 256KB device. Full re-trace clean otherwise — all ~1150 lines verified: evil_ble_uart.c (ISR pushes to stream buffer with zero timeout, worker 2048B stack safe ~520B peak with line_buf[512], DMB barriers in set_rx_callback correct for registration/deregistration ordering, volatile on connected/running, serial acquisition failure returns inert struct, teardown order correct: running→join→free→async_rx_stop→deinit→release→stream_free, expansion lifecycle correct), evil_ble_scanner.c (parse_mac sscanf 6 hex bytes + snprintf MAC 17+NUL in 18B exact fit, build_adv_from_name bounded by EXTRA_BEACON_MAX_DATA_SIZE-2=29 bytes, parse_scanbt_line text_buf[32] strncpy+NUL correct, MAC disambiguation at tlen≥17 correct, djb2 hash uint8_t cast prevents sign extension, placeholder MAC "DE:AD:%02X:%02X:%02X:%02X" 16+NUL in 18B, scanner_rx_cb NULL context guard present, volatile scanning check before mutex, dedup by MAC bounded at 32, struct copy under mutex, callback fires outside mutex, total stack in callback ~89B + frames ≈ 630B on 2048B), evil_ble.c (3 views lifecycle correct: 2 Submenus + 1 TextBox, add/remove/free order correct, clone_menu_label[48] fits 28-char max, device_labels[i][64] fits 39-char max, status_buf[256] fits 110-char max, GapExtraBeaconConfig with Random address type correct for cloning, adv payload bounded by EXTRA_BEACON_MAX_DATA_SIZE, beacon stop on both explicit action and app_free, teardown order correct: beacon→scanner→UART→remove_views→free_dispatcher→free_objects→close_record, back-button handling correct for all 3 views, navigation exits cleanly). Stack: main ~500/4096, UART worker ~630/2048. Prior fixes verified present: NULL context guard (2026-04-11), volatile scanning+connected (2026-04-11). Known characteristics: evil_ble_uart_is_connected() declared but never called (same as rogue_ap_detector), "(unknown)" dedup check vestigial (never generated by parser, harmless), inline adv construction in start_clone duplicates build_adv_from_name logic (maintenance risk but not correctness issue), main menu label not refreshed when returning from scan (cosmetic UX). No SD card I/O. No new cross-app issues.
- **evil_portal**: Non-FAP resource directory. Re-audited 2026-05-20. **HTML portals** (9 files): All clean — forms POST to `/` (relative), no XSS surface (zero JS), no external CDN references, mobile-friendly viewport. Minor: `trekistry_escape.html` has dead `https://trekistry.com` links (no internet in captive portal); `hotel_wifi.html`/`isp_maintenance.html`/`smart_home_setup.html` have `href="#"` placeholder footer links. **FPWN scripts** (5 files): CRITICAL — all 5 scripts describe Marauder attack sequences but never send the attack commands (`evilportal`, `sourapple`, `swiftpair`, `sniffpmkid`, `sniffprobe`, `karma`). FlipperPwn's payload engine has no `WIFI_CMD` for arbitrary Marauder commands — only `WIFI_SCAN`/`WIFI_DEAUTH`/`WIFI_WAIT`/`WIFI_RESULT` are implemented. Scripts are non-functional until a `WIFI_CMD` feature is added (Tier 7 task). **Python tools** (2 files): Fixed `import struct` unused in `marauder_serial.py`. Fixed `int | None` Python 3.10+ syntax in `pcap_capture.py` (added `from __future__ import annotations` for 3.8+ compat).

### Module/Payload Audit (last full audit: 2026-03-30, re-audit: 2026-05-15)

**flipperpwn_modules/** — 21 `.fpwn` modules:

All 21 modules: **Fixed** GUI r DELAY from 300ms to 500ms (2026-05-15). Windows Run dialog can take 400-700ms on machines with AV hooks.

| Module | Status | Findings |
|--------|--------|----------|
| recon/sys_info | Audited | Clean. Linux `CTRL ALT t` only works in GNOME (known limitation). |
| recon/av_detect | **Fixed** | 3 lines exceeded 512-byte limit (942, 600, 515 chars). Split into multi-line. |
| recon/network_enum | Audited | Clean. Uses modern iproute2 commands on Linux. |
| recon/wifi_enum | Audited | Clean. Minor: Linux reads wpa_supplicant.conf which may include passwords. |
| recon/wifi_scan_report | Audited | Clean. Requires ESP32. |
| recon/port_scan_report | **Fixed** | Added quoted SSID syntax `"{{SSID}}"` (2026-05-17). WIFI_JOIN parser now supports quoted SSIDs for names with spaces. |
| credential/wifi_harvest | **Fixed** | Linux `sudo` → `sudo -n` to prevent password prompt hang (2026-05-17). MAC hardcodes en0 for WiFi (acceptable — en0 is WiFi on 99%+ of consumer Macs, fails gracefully if not). |
| credential/ssh_keys | Audited | WIN line 490/512 bytes (near limit). |
| credential/env_dump | Audited | Clean. |
| credential/browser_creds | **Fixed** | WIN line was 723 chars (split 2026-03-30). MAC line 497/512 bytes (near limit). |
| credential/fake_login | **Fixed** | Cancel button infinite loop: ShowDialog always returned text (2026-05-15). Now checks DialogResult, returns null on Cancel, loop breaks. Also replaced deprecated Get-WmiObject with Get-CimInstance. |
| exploit/reverse_shell_tcp | Audited | Clean. Defaults safe (10.0.0.1). STRING at 326/512 bytes. |
| exploit/reverse_shell_dns | Audited | Clean. Defaults safe (10.0.0.1:53). STRING at 430/512 bytes (near limit). |
| exploit/download_exec | **Fixed** | MAC/LINUX `curl` missing URL quoting (2026-05-15). Added single quotes around `{{PAYLOAD_URL}}`. |
| exploit/msfvenom_stager | Audited | Clean. WIN-only. Defaults safe. |
| exploit/uac_bypass_fodhelper | **Fixed** | COMMAND in double-quotes broke on typical PS strings (2026-05-15). Changed to single quotes. |
| exploit/evil_twin | **Fixed** | Dead TARGET_SSID option removed (2026-05-15). LINUX xdg-open URL now quoted. |
| post/add_user | **Fixed** | Quoted USERNAME in 4 commands (2026-05-17). Replaced deprecated `wmic` with `Set-LocalUser` (2026-05-17). |
| post/disable_defender | Audited | Clean. Documents Tamper Protection limitation. |
| post/persist_schtask | **Fixed** | Falsely claimed no admin needed (2026-05-15). `/sc onlogon` requires admin. Updated docs and changed PAYLOAD_PATH default from C:\Windows\Temp to %TEMP%. |
| post/persist_startup | **Fixed** | OPTION default had spaces (2026-03-30). MAC plist XML-escaping issue for PAYLOAD_CMD with special chars (documented, not fixed). |

- **badusb_pro_sample_scripts/**: 3 `.ds` files — re-audited 2026-05-15. Clean. Uses OS_DETECT, LED_CHECK, IF/END_IF correctly.
- **ccid_emulator_sample_cards/**: 2 `.ccid` files — audited 2026-03-30, re-checked 2026-07-19. TLV structures valid, PIV AID correct. `piv_emulator.ccid`'s 62-byte CHUID response (line 18) was being silently truncated at load time by the 32-byte parser cap — fixed in the parser (issue #59), not in the file; the file itself is correct.
- **evil_portal/**: 9 HTML portals, 5 .fpwn scripts, 2 Python tools — audited 2026-03-30. All clean.

### Documentation Status (2026-04-04)

- **Per-app READMEs**: All 13 FAPs + evil_portal now have READMEs. Completed 2026-04-04 (added rogue_ap_detector, ble_scanner, evil_ble, subghz_jammer, uart_sniff, rayhunter_client).
- **CHANGELOG.md**: Created 2026-04-05. Retroactive entries from all 50 commits, organized by date with fix/feat/refactor/chore/docs categories.
- **Per-file header comments**: Completed 2026-04-05. All 39 `.c` files now have header comments explaining module purpose.

## Review Priority Queue

Priority by complexity (review more complex apps more frequently):

1. `flipperpwn` (~8,200 lines) — review every 2-3 sessions
2. `nfc_fuzzer` (~3,350 lines) — review every 3-4 sessions
3. `badusb_pro` (~2,900 lines) — review every 3-4 sessions
4. `hid_exfil` (~2,000 lines) — reviewed 2026-05-19
5. `spi_flash_dump` (~1,900 lines) — reviewed 2026-05-20
6. `ccid_emulator` (~1,600 lines)
7. `rogue_ap_detector` (~1,400 lines) — reviewed 2026-05-17
8. `ble_scanner` (~1,300 lines)
9. `rayhunter_client` (~1,200 lines)
10. `evil_ble` (~1,150 lines)
11. `subghz_spectrum` (~860 lines) — reviewed 2026-04-27
12. `uart_sniff` (~790 lines) — reviewed 2026-04-27
13. `subghz_jammer` (~730 lines) — reviewed 2026-05-04

Next session: `ccid_emulator` (20 days since 2026-04-30, 1600 lines) or `ble_scanner` (19 days since 2026-05-01, 1300 lines) or `flipperpwn` (12 days since 2026-05-08, 8200 lines — high complexity, frequent rotation) or `rayhunter_client` (18 days since 2026-05-02, 1200 lines).
